{
  "name": "@champagneswap/config",
  "private": false,
  "scripts": {
    "publish": "yarn workspaces foreach npm publish --tolerate-republish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "semver": "^7.3.5"
  }
}
